go mail:
    user.switcher_focus("Google Chrome")
    sleep(200ms)
    key(ctrl-1)

go files:
    key(super-8)

close files:
    key(cmd-w)

go terminal:
    user.switcher_focus("terminator")

focus last:
    key(alt-tab)
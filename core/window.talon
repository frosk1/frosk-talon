go mail:
    user.switcher_focus("Google Chrome")
    sleep(200ms)
    key(ctrl-1)

go files:
    key(super-8)

close files:
    key(cmd-w)

go term:
    user.switcher_focus("Terminator")

go code:
    user.switcher_focus("Code")

go web:
    user.switcher_focus("Google Chrome")

go last:
    key(alt-tab)

go chat:
    user.switcher_focus("Google Chrome")
    sleep(200ms)
    key(ctrl-3)